void immediateSmaller(vector<int>& a)
{
    int n=a.size();
	vector<int>ans;
    for(int i=0;i<n;i++)
    {
        if(a[i+1]<a[i])
        {
            a[i]=a[i+1];
        }
        else{
            a[i]=-1;
        }
    }
    a[n-1]=-1;
}
