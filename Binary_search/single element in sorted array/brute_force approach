int singleNonDuplicate(vector<int> &arr) {
	 int n = arr.size();
	 int count;
	 for(int i=0;i<n;i++)
	 {
		 if(arr[i]==arr[i-1] || arr[i]==arr[i+1])
		 {
			count++; 
		 }
		 else
		 return arr[i]; 
	 }
	 
}
