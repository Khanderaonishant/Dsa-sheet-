vector<int> moveZeros(int n, vector<int> a) {
   int j=0;
    for(int i=0;i<n;i++)
    {
        if(a[i]!=0)
        {
            a[j++]=a[i];
        }

    }
    for ( j=j;j < n; j++) {
        a[j] = 0;
    }
    return a;
}
