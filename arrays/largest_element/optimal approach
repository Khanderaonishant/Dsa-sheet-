#include <bits/stdc++.h> 
int largestElement(vector<int> &arr, int n) {
    int maxelement=0;
  //int n=arr.size();   
    for(int i=0;i<n;i++)
    {
       maxelement=max(maxelement,arr[i]); 
    }
    return maxelement;
}


